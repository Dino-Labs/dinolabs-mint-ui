import React from 'react';

export const OrderExpired = () => (
    <React.Fragment>
        <h1>
            Your order is expired
        </h1>
        <p>Unfortunately we didn't recieved payment in provided time. Your order expired.</p>
    </React.Fragment>
);